# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binsry, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# Do not include the resulting binary itself
api

# Do not include results of the test coverage tool
coverage.out
cover.xhtml
